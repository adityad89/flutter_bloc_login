name: flutter_bloc_login
description: A new Flutter project.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^7.1.0
  flow_builder: ^0.0.4
  google_fonts: ^2.1.0
  equatable: ^2.0.0
  font_awesome_flutter: ^9.1.0
  authentication_repository:
    path: packages/authentication_repository
  form_inputs:
    path: packages/form_inputs

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.1.0

flutter:
  uses-material-design: true
  assets:
    - assets/
